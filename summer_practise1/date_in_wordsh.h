#ifndef DATE_IN_WORDS_H
#define DATE_IN_WORDS_H

//підключення заголовних файлів
#include <stdio.h>
#include <locale.h>

/**
* Введене число(день) перетворити у слово(-а)
*
* @param day -введене число
* @return Не повертає значення
*/
int func_day(day);

/**
* ведене число(місяць) перетворити у слово
*
* @param month -введене число
* @return Не повертає значення
*/
int func_month(month);

#endif
